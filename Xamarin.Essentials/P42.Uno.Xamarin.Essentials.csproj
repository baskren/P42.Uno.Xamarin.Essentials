<Project Sdk="MSBuild.Sdk.Extras/3.0.23">
  <PropertyGroup>
    <TargetFrameworks>netstandard2.0;Xamarin.iOS10;Xamarin.Mac20;MonoAndroid11.0;</TargetFrameworks>
    <TargetFrameworks Condition=" '$(OS)' == 'Windows_NT' AND '$(USERNAME)' == 'ben' ">uap10.0.18362;$(TargetFrameworks);</TargetFrameworks>
 </PropertyGroup>
  <!--
  <ItemGroup>
    <UnoRuntimeProjectReference Include="P42.Uno.Xamarin.Essentials.Wasm.csproj" />
  </ItemGroup>
  
  <ItemGroup>
    <Content Include="buildTransitive\**">
      <PackagePath>buildTransitive</PackagePath>
      <Pack>true</Pack>
    </Content>
  </ItemGroup>
  -->


  <!-- WASM -->
  <Import Project=".\P42.Uno.Xamarin.Essentials.Wasm.props" Condition=" $(TargetFramework.StartsWith('net')) " />

  <!-- UWP -->
  <Import Project=".\P42.Uno.Xamarin.Essentials.UWP.props" Condition=" $(TargetFramework.StartsWith('uap10.0')) " />

  <!-- ANDROID -->
  <Import Project=".\P42.Uno.Xamarin.Essentials.Android.props" Condition=" $(TargetFramework.StartsWith('MonoAndroid')) " />

  <!-- IOS -->
  <Import Project=".\P42.Uno.Xamarin.Essentials.iOS.props" Condition=" $(TargetFramework.StartsWith('Xamarin.iOS')) " />

  <!-- MACOS -->
  <Import Project=".\P42.Uno.Xamarin.Essentials.MacOS.props" Condition=" $(TargetFramework.StartsWith('Xamarin.Mac20')) " />

  
</Project>
